#include <stdio.h>

/**
 * main -prints single digit numbers starting of base 10 from 0
 *
 * Return: 0
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	putchar ('\n');
	return (0);
}
